{
  "name": "reef-for-all-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
